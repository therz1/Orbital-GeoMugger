# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. Install Flutter so the machine knows what 'flutter' is
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
      
      # 2. Make sure your script is executable
      - run: chmod +x deploy.sh

      # 3. Now run your script
      - run: deploy.sh

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_GEOMUGGER_18A74 }}
          channelId: live
          projectId: geomugger-18a74
